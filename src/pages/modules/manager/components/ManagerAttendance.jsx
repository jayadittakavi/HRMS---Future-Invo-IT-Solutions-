import React from 'react';
import { AttendanceContent } from '../../../attendance/Attendance';

const ManagerAttendance = () => {
    return (
        <AttendanceContent personal={false} />
    );
};

export default ManagerAttendance;
